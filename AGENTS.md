# Expo SDK

This project intentionally uses stable Expo SDK 54. Use the SDK 54 versioned documentation and run `npx expo install --check` after dependency changes. The previously generated SDK 58 documentation URL returns 404 and the available SDK 58 npm release is a canary, so it must not be used for production work.
